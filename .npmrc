# pnpm v10 ships a default minimumReleaseAge (~24h) that rejects freshly
# published packages. Disable it at the project level — lockfile integrity
# hashes and --frozen-lockfile still protect the supply chain.
minimum-release-age=0